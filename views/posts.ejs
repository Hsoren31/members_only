<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Posts</title>
  </head>
  <body>
    <a href="/">Back to Home</a>
    <% if (posts) {%>
    <ul>
      <% posts.forEach(function(post) { %>
      <li><%= post.message %> - anonymous user</li>
      <% }); %>
    </ul>
    <% } %>
  </body>
</html>
